駅間ナビ v43 検証結果
====================

基準版
------
作業元: 202609211910_ekikan_gps_42_33.zip
画面版: SILENT JOURNEY 42 → 43

実施済み
--------
- ZIPからv42を新規展開し、その内容を直接編集。古い版を作業元にしていない。
- index.html 内インラインJavaScriptを抽出し `node --check` で構文エラーなし。
- sw.js を `node --check` で構文エラーなし。
- 検索UIがメイン4タブより前に配置されていることを確認。
- 検索inputが type=search / inputmode=search / enterkeyhint=search を持つことを確認。
- 検索候補が ALL_GUIDE_ROUTES + guideStationNames() から生成され、別の駅リストを追加していないことを確認。
- 完全一致が存在する場合に score=0 の候補だけへ絞る処理を確認。
- 同じ短縮路線名が複数ある場合のみ区間名を付ける表示ロジックを確認。
- 候補1件で駅・路線を自動選択して方面欄へ進む処理を確認。
- 方面選択時に既存 guideBrowseRoute / guideBrowseStation / guideBrowseDirection へ同期し、既存 renderGuide() で描画する処理を確認。
- v42のエレベーターカード強調CSS / 静的・動的判定処理が残っていることを確認。
- Service Worker shell cache を43.0へ更新。動的オンライン案内のparser28キャッシュは解析仕様未変更のため維持。
- headless Chromiumでは localhost / file URL への直接遷移が実行環境ポリシーで遮断されたため、同一HTMLを `page.set_content()` で読み込むDOM E2Eを実施。日吉検索→東急目黒線選択→目黒方面選択→降車位置タブ自動移動、`新宿` 完全一致時の部分一致除外、`浜松町` から東京モノレール候補が出ること、検索欄がタブより上にあることを確認し `QUICK_SEARCH_E2E_PASS`。

安全側の仕様
--------------
- 駅名が完全一致すれば部分一致駅を混ぜず、誤選択を減らす。
- 検索は既存の降車位置対象路線だけを使う。
- 方面が未選択のまま自動表示しない。
- 実際の降車案内取得・方向判定はv42までの既存ロジックをそのまま再利用する。
